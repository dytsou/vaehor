{
  "missingSession": "Login required: set VAEHOR_SESSION_COOKIE (full cookie string, e.g. authjs.session-token=…) or VAEHOR_SESSION_TOKEN (session value only). After signing in to vaehor in your browser, copy the cookie from developer tools.",
  "searchNeedOneCriterion": "At least one of q, mimeType, modifiedTime, or minSize is required.",
  "tools": {
    "zee_health": {
      "title": "vaehor health",
      "description": "Calls GET /api/health (public). No session. Set VAEHOR_BASE_URL (default http://127.0.0.1:3000)."
    },
    "zee_public_config": {
      "title": "vaehor public config",
      "description": "Calls GET /api/config/public (public). No session. Returns public app settings (no secrets)."
    },
    "zee_metadata_lookup": {
      "title": "TMDB metadata by filename",
      "description": "Calls GET /api/metadata?filename=… (public). Resolves media title from filename and queries TMDB. No session."
    },
    "zee_whoami": {
      "title": "Current vaehor user",
      "description": "Calls GET /api/auth/me. Requires session (VAEHOR_SESSION_COOKIE or VAEHOR_SESSION_TOKEN). Returns current user and role, same as the web app."
    },
    "zee_files_list": {
      "title": "List files in folder",
      "description": "Calls GET /api/files. Requires session. Returns file list according to user permissions and folder rules (same as the web app)."
    },
    "zee_search": {
      "title": "Search files (folder scope)",
      "description": "Calls GET /api/search. Requires session. Search within a folder or with global filters (same query parameters as the web API)."
    },
    "zee_search_global": {
      "title": "Search files (global tree)",
      "description": "Calls GET /api/search/global. Requires session. Global search under the root tree (must be logged in)."
    }
  },
  "schema": {
    "filename": "File name or last path segment, e.g. Movie.2024.mkv",
    "folderId": "Google Drive folder ID; omit to use the server-configured root",
    "pageToken": "Pagination token",
    "refresh": "Force bypass cache (maps to ?refresh=true)",
    "q": "Keyword (name or full text, depending on searchType)",
    "folderIdSearch": "Search only under this folder",
    "searchType": "name or fullText; default name",
    "mimeType": "image | video | audio | pdf | folder",
    "modifiedTime": "today | week | month",
    "minSize": "Minimum file size (MB, numeric string)",
    "qGlobal": "Search keyword"
  }
}
