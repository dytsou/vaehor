# Exclude generated build artifacts from analysis.
# docs/api/website/index.html is emitted by `pnpm build:redocly` (Redoc bundle);
# its markup/CSS is vendor-generated and rewritten on every build, so linting it is noise.
sonar.exclusions=docs/api/website/**
